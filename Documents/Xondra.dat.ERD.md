# Xondra.dat — Entity Relationship Diagram

Generated by reading the SQLite `sqlite_master` schema directly from `Xondra.dat`.

`Xondra.dat` holds the backup run history and the file/directory inventory
that supports it: `Backup` runs, the `BackupSet` (and `BackupSetEmptyDir`)
rows produced by each run, a dedupe-friendly `File` table keyed by content
hash, and normalized lookup tables (`LocalDrive`, `LocalDirectory`,
`LocalFilename`) that the caches (`FileCache`, `DirCache`) and backup sets
point into instead of repeating strings.

## Diagram

```mermaid
erDiagram
    BACKUP {
        INTEGER ID PK
        TEXT ComputerGUID
        DATETIME StartDate
        DATETIME EndDate
        INTEGER FileCount "DEFAULT 0"
        INTEGER ErrorCount "DEFAULT 0"
        INTEGER LastDirID FK "implicit reference to LocalDirectory.ID"
        STRING LastDir
        INTEGER TargetFileCount
        DATETIME ProcessingDate
        STRING Status
        TEXT SettingsJSON
    }

    ERROR {
        INTEGER ID PK
        INTEGER BackupID FK "implicit reference to Backup.ID"
        STRING ProcedureName
        STRING Error
    }

    BACKUPSET {
        INTEGER ID PK
        INTEGER BackupID FK "implicit reference to Backup.ID, NOT NULL"
        INTEGER DirID FK "references LocalDirectory.ID, NOT NULL"
        INTEGER FileID FK "references File.ID, NOT NULL"
        INTEGER FilenameID FK "references LocalFilename.ID, NOT NULL"
        INTEGER DriveID FK "references LocalDrive.ID, NOT NULL"
        INTEGER Error
        INTEGER Attributes
        TEXT CreationTime
        TEXT LastWriteTime
    }

    BACKUPSETEMPTYDIR {
        INTEGER ID PK
        INTEGER BackupID FK "implicit reference to Backup.ID, NOT NULL"
        INTEGER DirID FK "references LocalDirectory.ID, NOT NULL"
        INTEGER DriveID FK "references LocalDrive.ID, NOT NULL"
        INTEGER Error
    }

    FILE {
        INTEGER ID PK
        CHAR OriginalFileHash "CHAR(128), NOT NULL"
        CHAR OrigHMACSHA512 "CHAR(128)"
        INTEGER Filesize
        BOOLEAN LocalVerified
        CHAR BackupHash "CHAR(128)"
        INTEGER FilesizeCompressed
    }

    LOCALDRIVE {
        INTEGER ID PK
        STRING Drive UK "NOT NULL"
    }

    LOCALDIRECTORY {
        INTEGER ID PK
        STRING Directory UK "NOT NULL"
    }

    LOCALFILENAME {
        INTEGER ID PK
        STRING Filename UK "NOT NULL"
    }

    FILECACHE {
        INTEGER ID PK
        STRING ComputerGUID "NOT NULL"
        STRING Drive "NOT NULL"
        STRING Directory "NOT NULL"
        STRING Filename
        DATETIME LastModifiedDateUTC
        BOOLEAN ArchiveBit
        INTEGER FileSize
        INTEGER LocalDriveID FK "implicit reference to LocalDrive.ID"
        INTEGER LocalDirectoryID FK "implicit reference to LocalDirectory.ID"
        INTEGER LocalFilenameID FK "implicit reference to LocalFilename.ID"
        INTEGER Backup "DEFAULT 1"
        INTEGER Attributes
        TEXT CreationTime
        TEXT LastWriteTime
    }

    DIRCACHE {
        INTEGER ID PK
        STRING ComputerGUID "NOT NULL"
        STRING Drive "NOT NULL"
        STRING Directory "NOT NULL"
        INTEGER LocalDriveID FK "implicit reference to LocalDrive.ID"
        INTEGER LocalDirectoryID FK "implicit reference to LocalDirectory.ID"
    }

    FILELIST {
        INTEGER ID PK
        STRING Directory
        STRING Filename
    }

    BACKUP ||--o{ BACKUPSET          : "BackupID"
    BACKUP ||--o{ BACKUPSETEMPTYDIR  : "BackupID"
    BACKUP ||--o{ ERROR              : "BackupID"
    LOCALDIRECTORY ||--o{ BACKUP     : "LastDirID"

    LOCALDIRECTORY ||--o{ BACKUPSET         : "DirID"
    LOCALDRIVE      ||--o{ BACKUPSET         : "DriveID"
    FILE             ||--o{ BACKUPSET         : "FileID"
    LOCALFILENAME    ||--o{ BACKUPSET         : "FilenameID"

    LOCALDIRECTORY ||--o{ BACKUPSETEMPTYDIR : "DirID"
    LOCALDRIVE      ||--o{ BACKUPSETEMPTYDIR : "DriveID"

    LOCALDRIVE      ||--o{ FILECACHE : "LocalDriveID"
    LOCALDIRECTORY ||--o{ FILECACHE : "LocalDirectoryID"
    LOCALFILENAME    ||--o{ FILECACHE : "LocalFilenameID"

    LOCALDRIVE      ||--o{ DIRCACHE : "LocalDriveID"
    LOCALDIRECTORY ||--o{ DIRCACHE : "LocalDirectoryID"
```

`FileList` has no columns that reference any other table and no relationships
were found to or from it — it appears to be a standalone/legacy table and is
shown unconnected in the diagram above.

## Notes

- **Enforced foreign keys** (declared with `REFERENCES` in `CREATE TABLE`):
  `BackupSet.DirID → LocalDirectory.ID`, `BackupSet.FileID → File.ID`,
  `BackupSet.FilenameID → LocalFilename.ID`, `BackupSet.DriveID →
  LocalDrive.ID`, `BackupSetEmptyDir.DirID → LocalDirectory.ID`,
  `BackupSetEmptyDir.DriveID → LocalDrive.ID`.
- **Implicit relationships** (matching naming convention/data, not declared
  as a foreign key in the schema): `BackupSet.BackupID`,
  `BackupSetEmptyDir.BackupID`, and `Error.BackupID` all point to
  `Backup.ID`; `Backup.LastDirID` points to `LocalDirectory.ID`;
  `FileCache.LocalDriveID` / `LocalDirectoryID` / `LocalFilenameID` and
  `DirCache.LocalDriveID` / `LocalDirectoryID` point to `LocalDrive.ID` /
  `LocalDirectory.ID`. `FileCache` also carries denormalized `Drive` /
  `Directory` / `Filename` text columns alongside the normalized ID columns.
- **Deduplication design**: `File` is keyed by `OriginalFileHash` (with a
  supporting index `ix_file_OriginalFileHash`) rather than by path, so
  identical file content backed up from multiple locations/times is expected
  to collapse to one `File` row, with many `BackupSet` rows pointing at it.
- `sqlite_sequence` is SQLite's internal autoincrement bookkeeping table and
  is omitted from the diagram.
- Additional index: `ix_backupset_all` on `BackupSet (BackupID, DirID,
  FileID, FilenameID, DriveID)`. `LocalDrive`, `LocalDirectory`, and
  `LocalFilename` each have a unique index on their text column
  (`Drive`/`Directory`/`Filename`) in addition to their `ID` primary key.
